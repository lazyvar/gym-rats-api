defmodule GymRats.Repo.ChallengeScoreRepo do
  import Ecto.Query, only: [where: 3]

  alias GymRats.Repo
  alias GymRats.Model.Challenge

  def leader(challenge) do
  end

  def current_account_score(challenge) do
  end
end
