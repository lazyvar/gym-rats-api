defmodule GymRatsWeb.AccountControllerTest do
  use GymRatsWeb.ConnCase

  alias GymRats.Model.Account

  describe "create/2" do
    test "Creates and responds with the newly created user if attributes are valid", %{conn: conn} do

    end

    test "Returns an error and does not create a user if attributes are invalid" do

    end
  end
end