image: elixir:latest

services:
  - postgres:latest

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  
variables:
  MIX_ENV: "test"
  
mix:
  script:
    - mix test
